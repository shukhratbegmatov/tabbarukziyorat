#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../@intlify/vue-i18n-bridge/bin/switch.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../@intlify/vue-i18n-bridge/bin/switch.js" "$@"
  ret=$?
fi
exit $ret
